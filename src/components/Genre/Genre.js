const Genre = ({genre}) => {

    // const {short_name}=genre;

    return (
        <div>
            <div>
                {/*{short_name}*/}
            </div>
        </div>
    );
};

export {Genre};
